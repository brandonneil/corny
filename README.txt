-- SUMMARY --

The corny module creates a ctools content type plugin for DrupalCorn badges.

It allows you to select an attendee, sponsor, or individual sponsor badge type
in addition to the year (2012, 2013, 2014).

Requires the ctools module.
